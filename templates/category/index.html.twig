{% extends 'base.html.twig' %}
{% block body %}
  <div class="container">

        <table class="table">
        <tr>
            <th>Nom</th>
            <th>Description</th>
            <th>Slug</th>
            <th>Editer</th>
            <th>Supprimer</th>
        </tr>

        {% for category in categories %}
        <tr>
            <td>
                {{category.name}}
            </td>
            <td>
                {{category.description}}
            </td>
            <td>
                {{category.slug}}
            </td>
            <td>
                <a href="{{path('editCategory', {'id':category.id})}}">Editer la catégorie</a>
            </td>

                        <td>
                <a href="{{path('editCategory', {'id':category.id})}}">Editer la catégorie</a>
            </td>
        </tr>

        {% endfor %}
    </table>

  </div>
{% endblock %}