{% extends 'base.html.twig' %}

{% block title %}Users{% endblock %}

{% block body %}

  <div class="container">
    <table class="table">
      <tr>
        <th>
          Email
        </th>
        <th>
          Mdp
        </th>
        <th>
          Role
        </th>
        <th>
          
        </th>
        <th>
          
        </th>
        <th>
          
        </th>
      </tr>

      {% for user in users %}
        <tr>
          <td>
            {{ user.email }}
          </td>
          <td>
            {{ user.password }}
          </td>
          <td>{{ user.roles ? user.roles|json_encode : '' }}</td>

              <td>
            <a href="{{ path('showUser', {'id': user.id}) }}">voir</a>
          </td>

          <td>
            <a href="{{ path('editUser', {'id': user.id}) }}">modifier</a>
          </td>

              <td>
            <a href="{{path('deleteUser', {'id': user.id})}}">
              supprimer
            </a>
          </td>
        </tr>
      {% endfor %}
    </table>
  </div>

{% endblock %}

